/**
 * The React Native entry point.
 *
 * Kept as a one-line re-export so everything the app actually does lives under
 * src/, alongside the code it collaborates with, rather than at the repo root
 * where the template happened to put it.
 */

export { default } from './src/App';
