{
  "name": "functions",
  "description": "Cloud Functions for the skill-gaming trial — money, target engine, score validation.",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "test": "node --test test/",
    "db:reset": "node scripts/resetDb.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {}
}
