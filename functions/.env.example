# Supabase Postgres — TRANSACTION-MODE POOLER. Port 6543, not 5432: db.js is
# written against transaction mode (no advisory locks, no session state).
PG_HOST=aws-0-<region>.pooler.supabase.com
PG_PORT=6543
PG_DATABASE=postgres
PG_USER=postgres.<project-ref>
PG_PASSWORD=<password>
